FLASK_ENV=development
SECRET_KEY=change-me
DATABASE_URL=postgresql+psycopg2://contract_user:contract_pass@localhost:5433/contract_db

# Free tier at sentry.io — create a Flask project, paste its DSN here.
# Leave blank to disable error tracking entirely (default for local dev).
SENTRY_DSN=
